import 'package:flutter/material.dart';

class AdventureEscapes extends StatelessWidget {
  const AdventureEscapes({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('AdventureEscapes'),
      ),
    );
  }
}