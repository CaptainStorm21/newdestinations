import 'package:flutter/material.dart';

class WalkingAdventures extends StatelessWidget {
  const WalkingAdventures({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('WalkingAdventures'),
      ),
    );
  }
}