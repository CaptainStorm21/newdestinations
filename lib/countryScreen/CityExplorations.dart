import 'package:flutter/material.dart';

class CityExplorations extends StatelessWidget {
  const CityExplorations({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('CityExplorations'),
      ),
    );
  }
}