import 'package:flutter/material.dart';

class NightlifeDiscoveries extends StatelessWidget {
  const NightlifeDiscoveries({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('NightlifeDiscoveries'),
      ),
    );
  }
}