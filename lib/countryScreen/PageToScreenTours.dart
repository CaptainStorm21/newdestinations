import 'package:flutter/material.dart';

class PageToScreenTours extends StatelessWidget {
  const PageToScreenTours({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('PageToScreenTours'),
      ),
    );
  }
}