import 'package:flutter/material.dart';

class DiscoveryDen extends StatelessWidget {
  const DiscoveryDen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('DiscoveryDen'),
      ),
    );
  }
}