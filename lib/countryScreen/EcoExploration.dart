import 'package:flutter/material.dart';

class EcoExplorations extends StatelessWidget {
  const EcoExplorations({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('EcoExplorations'),
      ),
    );
  }
}