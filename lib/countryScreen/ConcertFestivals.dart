import 'package:flutter/material.dart';

class ConcertsFestivals extends StatelessWidget {
  const ConcertsFestivals({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('ConcertsFestivals'),
      ),
    );
  }
}