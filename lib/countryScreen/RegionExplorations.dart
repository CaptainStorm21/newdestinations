import 'package:flutter/material.dart';

class RegionExplorations extends StatelessWidget {
  const RegionExplorations({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('RegionExplorations'),
      ),
    );
  }
}