import 'package:flutter/material.dart';

class MultiDayTours extends StatelessWidget {
  const MultiDayTours({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('MultiDayTours'),
      ),
    );
  }
}