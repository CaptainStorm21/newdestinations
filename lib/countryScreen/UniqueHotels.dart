import 'package:flutter/material.dart';

class UniqueHotels extends StatelessWidget {
  const UniqueHotels({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('UniqueHotels'),
      ),
    );
  }
}