import 'package:flutter/material.dart';

class SpaRetreats extends StatelessWidget {
  const SpaRetreats({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Spa Retreats'),
      ),
    );
  }
}