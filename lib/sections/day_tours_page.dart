import 'package:flutter/material.dart';

class DayToursPage extends StatelessWidget {
  const DayToursPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      height: 400,
      child: Center(child: Text('Day Tours')),
    );
  }
}
