import 'package:flutter/material.dart';

class MultiDayTravelsPage extends StatelessWidget {
  const MultiDayTravelsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      height: 400,
      child: const Center(child: Text('Multi-Day Travels')),
    );
  }
}
