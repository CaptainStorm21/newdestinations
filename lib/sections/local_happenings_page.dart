import 'package:flutter/material.dart';

class LocalHappeningsPage extends StatelessWidget {
  const LocalHappeningsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      height: 400,
      child: const Center(child: Text('Local Happenings')),
    );
  }
}
