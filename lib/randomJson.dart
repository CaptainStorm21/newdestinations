import 'package:flutter/material.dart';

class RandomJson extends StatelessWidget {
  const RandomJson({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Random JSON Data Here'),
    );
  }
}